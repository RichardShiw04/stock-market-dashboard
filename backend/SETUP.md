# Backend setup

See [the project README](../README.md) for environment settings, schema choices and frontend setup.

Run `uvicorn main:app --reload` from this directory after installing requirements.txt and configuring .env. No credentials are included.
